<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Отправка данных на сервер</title>
    <style>
body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        #container {
            text-align: center;
        }

        #inputField {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 10px;
        }

        #dataInput {
            margin-right: 10px;
        }

        #responseBlocks {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .responseBlock {
            border: 1px solid #ccc;
            padding: 10px;
            margin: 5px;
            width: 300px;
            text-align: left;
        }

        .responseBlock p {
            margin: 5px 0;
        }

        #inputFieldsContainer {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 20px;
        }

        .inputField {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 10px;
        }

        .inputField label {
            margin-right: 10px;
        }

        .inputField input {
            margin-right: 10px;
        }
    </style>
</head>
<body>
<div id="container">
    <div id="inputFieldsContainer">
        <div class="inputField">
            <label for="number1">Число 1:</label>
            <input type="text" id="number1" placeholder="Введите число 1">
        </div>
        <div class="inputField">
            <label for="number2">Число 2:</label>
            <input type="text" id="number2" placeholder="Введите число 2">
        </div>
        <div class="inputField">
            <label for="number3">Число 3:</label>
            <input type="text" id="number3" placeholder="Введите число 3">
        </div>
        <div class="inputField">
            <label for="number4">Число 4:</label>
            <input type="text" id="number4" placeholder="Введите число 4">
        </div>
    </div>

    <div id="inputField">
        <label for="dataInput">Данные:</label>
        <input type="text" id="dataInput" placeholder="Введите данные">
        <button onclick="sendData()">Отправить на сервер</button>
    </div>

    <div id="responseBlocks"></div>
</div>

<script>
    async function sendData() {
        var number1 = document.getElementById('number1').value;
        var number2 = document.getElementById('number2').value;
        var number3 = document.getElementById('number3').value;
        var number4 = document.getElementById('number4').value;
        var inputData = document.getElementById('dataInput').value;

        try {
            const response = await fetch('http://localhost:9999/', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ number1, number2, number3, number4, inputData }),
            });

            if (!response.ok) {
                throw new Error('Ошибка при отправке данных на сервер');
            }

            const responseData = await response.json();

            // Далее обрабатываем ответ от сервера и создаем блок с информацией
            var responseBlocks = document.getElementById('responseBlocks');

            var newBlock = document.createElement('div');
            newBlock.className = 'responseBlock';

            var id = responseData.id;
            var example = 'Пример: ' + responseData.inputData;
            var answer = 'Ответ: ' + responseData.serverResponse;
            var solveTime = 'Время решения: ' + responseData.solveTime;
            var status = 'Статус: ' + responseData.status;

            newBlock.innerHTML = `<p>${id}</p><p>${example}</p><p>${answer}</p><p>${status}</p><p>${solveTime}</p>`;

            responseBlocks.appendChild(newBlock);
        } catch (error) {
            console.error('Ошибка:', error.message);
        }
    }
</script>
</body>
</html>
